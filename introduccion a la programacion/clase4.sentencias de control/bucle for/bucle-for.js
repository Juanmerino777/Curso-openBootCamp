 // bucle for:

//      inicialicacion     comparacion    ejecucion
for  (var numeroFor = 0; numeroFor <= 3; numeroFor++) {
  console.log(numeroFor);
}

/*los bucles for tienen tres partes 
1ra INICIALICACION: es donde declaramos una variable con un valor.
2da CONPARACION: es donde ordenamos un condicion a seguir.
3ra EJECUCION: es donde cololcamos la orden a realizar en caso que la condicion se cumpla.
todo lo anterior se separa por (;) y entre corchetes se imprime en consola   */ 